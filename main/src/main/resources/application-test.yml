spring:
    h2:
        console:
            enabled: false

    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://localhost:3307/shopping_mall?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
        username: shopping
        password: shopping1234

    jpa:
        hibernate:
            ddl-auto: update
        show-sql: false

    data:
        redis:
            host: localhost
            port: 6379
